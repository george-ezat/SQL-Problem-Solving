SELECT FLOOR(AVG(POPULATION))
FROM CITY;

-- SQL Server

SELECT AVG(POPULATION)
FROM CITY;

-- In SQL Server you can use any of them
-- In SQL Server if you take the average of an INT column, the result will be truncated to an integer.